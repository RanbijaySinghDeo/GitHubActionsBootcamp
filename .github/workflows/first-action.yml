name: First Workflow
on: workflow_dispatch
jobs:
  First-Job:
    runs-on: ubuntu-latest
    steps:
      - name: print name
        run: echo "Hello world"
      - name: print goodbye
        run: echo "Goodbye!"
